{
    "name": "webpack-4-object-assign",
    "version": "0.0.1",
    "description": "Used to test webpack config",
    "scripts": {
        "dev": "rm -rf build && NODE_ENV=development webpack",
        "prod": "rm -rf build && NODE_ENV=production webpack",
        "both": "rm -rf build && NODE_ENV=development webpack && NODE_ENV=production webpack"
    },
    "dependencies": {
        "babel-loader": "^7.1.4",
        "webpack": "^4.6.0",
        "webpack-cli": "^2.0.15",
        "webpack-merge": "^4.1.2"
    }
}
